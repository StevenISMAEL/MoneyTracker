<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso de Internet (Importante para más adelante) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoneyTracker_silarac"
        tools:targetApi="31">

        <!-- 1. Pantalla de Configuración (INICIO) -->
        <activity
            android:name=".ui.SetupActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. Dashboard Principal -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false" />

        <!-- 3. Pantalla de Agregar Transacción (NUEVA) -->
        <!-- ESTO ES LO QUE FALTABA -->
        <activity
            android:name=".ui.AddTransactionActivity"
            android:parentActivityName=".ui.MainActivity"
            android:exported="false" />
        <activity
            android:name=".ui.StatisticsActivity"
            android:parentActivityName=".ui.MainActivity"
            android:exported="false" />

    </application>

</manifest>